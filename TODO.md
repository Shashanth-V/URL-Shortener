# Build & Run TODO

- [x] Scan and understand the project
- [x] Fix `frontend-test-submission/package.json` (`react-scripts` invalid version)
- [x] Fix `logging-middleware/package.json` (add start script, avoid port conflict)
- [x] Install dependencies in `frontend-test-submission`
- [x] Install dependencies in `logging-middleware`
- [x] Start backend middleware
- [x] Start frontend app
- [x] Fix runtime errors if any
- [x] Verify both are running correctly
- [x] Turn logging-middleware into real URL shortener API (POST /shorten, GET /:code)
- [x] Add CORS to backend so frontend can call it
- [x] Wire frontend to call backend API instead of generating fake URLs
- [x] Update short link in UI to point to backend
- [x] Test full flow: shorten → click link → redirects to original URL

